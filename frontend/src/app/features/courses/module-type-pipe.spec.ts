import { ModuleTypePipe } from './module-type-pipe';

describe('ModuleTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleTypePipe();
    expect(pipe).toBeTruthy();
  });
});
